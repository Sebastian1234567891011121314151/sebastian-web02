function setup() {
new Canvas (600,500)
background('pink')
}

function draw()  {
textAlign(CENTER,CENTER )
    textSize(25);
    fill("white");
    text('Guess the hidden Word!')
    text('Attemps')
}